# Controls what `gcloud builds submit` uploads as build context.

# Dependencies (reinstalled inside the image via npm ci)
node_modules
**/node_modules

# Build artifacts
.next
out
build

# Nested, unrelated project (do NOT ship its node_modules)
voyage-ai-3d

# VCS / IDE / OS
.git
.gitignore
.vscode
.idea
.DS_Store

# Secrets and local env
.env
.env.*

# Logs and misc
npm-debug.log*
yarn-debug.log*
yarn-error.log*
*.log
terminals

# Docs / planning
*.md
.cursor
